static/dist
node_modules
.env
*.pem

# Health records are real PHI - keep them out of the repo entirely.
# Preferred location is outside the working tree (e.g. ~/health-records/);
# these rules are a safety net for anything accidentally written here.
data/
*.sqlite
.DS_Store
.playwright-mcp/
